spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##         show_sql: true -- sout로 찍은 로그라 사용 지양
#        format_sql: true
#--- 설정했을 때: HikariPool-1 - Added connection conn0: url=jdbc:h2:mem:test
# 설정이 없으면 스프링부트가 기본적으로 메모리모드로 돌린다
# HikariPool-1 - Added connection conn0: url=jdbc:h2:mem:9b83cb96-18fd-4e95-b96f-becd2d716cec user=SA

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace